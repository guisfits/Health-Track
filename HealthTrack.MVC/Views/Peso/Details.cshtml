@model HealthTrack.MVC.ViewModels.PesoViewModel

@{
    ViewBag.Title = "Detalhes";
}

<div>
    <h4>@ViewBag.Title</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.ValorPeso)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.ValorPeso)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.DataHora)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.DataHora)
        </dd>

    </dl>
</div>
<p>
    <div class="btn-group">
        @using (Html.BeginForm("Delete", "Peso", new { id = Model.Id }, FormMethod.Post))
        {
            @Html.AntiForgeryToken()
            <input type="submit" value="Deletar" class="btn btn-danger" />
        }
        @Html.ActionLink("Voltar para listagem", "Index")
    </div>
</p>
