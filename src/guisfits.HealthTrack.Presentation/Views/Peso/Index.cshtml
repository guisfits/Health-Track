@model IEnumerable<guisfits.HealthTrack.Application.ViewModels.PesoViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Pesos</h2>

<p>
    <a class="btn btn-success" href="@Url.Action("Create")"><i class="fa fa-plus" aria-hidden="true"></i> Adicionar</a>
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PesoValue)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DataHora)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PesoValue)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DataHora)
        </td>
        <td>
            @Html.Partial("_CRUD", item)
        </td>
    </tr>
}

</table>
