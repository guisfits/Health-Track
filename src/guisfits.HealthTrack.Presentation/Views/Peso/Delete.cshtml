@model guisfits.HealthTrack.Application.ViewModels.PesoViewModel

@{
    ViewBag.Title = "Deletar";
}

<h3>Quer realmente deletar este item?</h3>
<div>
    <h4>@ViewBag.Title</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.PesoValue)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.PesoValue)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.DataHora)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.DataHora)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color btn-group">
            <input type="submit" value="Deletar" class="btn btn-danger" />
            <a href="@Url.Action("Index")" class="btn btn-default">Cancelar</a>
        </div>
    }
</div>
